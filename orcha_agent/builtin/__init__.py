"""First-party plugins shipped with orcha-agent.

Importing this package is intentionally inert. The plugin loader discovers and
registers each child module through its ``register(PluginAPI)`` boundary.
"""
